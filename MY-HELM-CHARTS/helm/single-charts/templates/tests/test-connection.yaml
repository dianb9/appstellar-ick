apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "single-charts.fullname" . }}-test-connection"
  labels:
    {{- include "single-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "single-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
